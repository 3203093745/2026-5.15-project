# 2026-5.15-project
第一章
pasta_type = "spaghetti"//定义一个变量的方法
quantity = 80
print(pasta_type)
print(quantity)

pasta_type = "fusilli"//修改变量
quantity = 100
print(pasta_type)
print(quantity)

ingredient_name = "tomatoes"//字符串类型
 print(type(ingreddient_name))
 
ingredient_quantity = 2 //整数类型
 print(type(ingreddient_quantity))
 
new_ingredient_quantity = 1.5 //浮点数类型
 print(type(new_ingredient_quantity))
 
is_in_stock = True //布尔值类型，后面的"T"大写
 print(type(is_in_stock)

"Hi" + "There" = "HiThere"//运算
"Hi" * 3 ="HiHiHi"

garlic_cloves = 3 //定义一个整数变量
olive_oil_tbsp = 2.5 //定义一个浮点数变量
 print(olive_oil_tbsp)
new_olive_oil_tbsp = 2.5 + 1 //浮点数变量进行变化
 print(garlic_cloves)
 print(new_olive_oil_tbsp)

has_pasta = True //创建的布尔变量
has_garlic = False
 print(has_pasta)
 print(has_garlic)

print(type(olive_oil_tbsp)) //打印数据的变量类型使用type()
print(type(has_pasta))

第二章
cooking_instructions = """1: Boil water in a large pot //多行字符串变量
2: Add pasta and cook for 10 minutes
3: Drain and serve with sauce"""
print(cooking_instructions)

pasta_type = "pasta"
pasta_type = pasta_type.replace("pasta","fusilli pasta") //使用.replace("","")方法进行变量的转换

ingredient_one = "BASIL" //使用.lower()方法进行变量间的小写转换
ingredient_one = ingredient_one.lower()
 print(pasta_type)
 print(ingredient_one)

ingredients = ["pasta","tomatoes","garlic","basil","olive oil","salt"] //创建一个列表
 print(ingredients[0]) //输出的是"pasta"，因为第一个元素的索引号为0
 print(ingredients[3:]) //特定索引到列表末尾的所有元素，得到3、4、5元素
 print(ingredients[:3]) //从起始位置到特定位置
 print(ingredients[::2]) //从第一个元素开始隔一个元素进行抓取
 print(ingredients[1::3]) //从索引为1的第二个元素开始，隔三个值取得一个值

Dictionary = key-value
recipe = {"pasta":500,"tomatoes":400,"garlic":15} //花括号，键值对
print(recope["pasta"]) //方括号括起键名，而不是索引
print(recipe.values()) //获取字典中的所有值
print(recipe.keys()) //检索字典中的所有键
print(recipe) //查看整个字典
print(recipe.items()) //查看每个键值对且都以逗号分隔的值显示在括号内

 






















 

